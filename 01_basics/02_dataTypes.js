"use strict"; // treat all JS code asnewer version

// alert("hello") // we are using node, not browser 

//code readibility should be high

console.log("shivam");

let name = "Shivam"
let age = 20
let isLoggedIn = false

// number => 2 to the power 53
// bigint (for much bigger numbers)
// string => ""
// boolean => true /false 
// null => standalone value
// undefines => not assigned value
// symbol => component uniqueness
// objects 

console.log(typeof undefined);   //undefined
console.log(typeof null);   //object
console.log(typeof NaN);   //Number





//